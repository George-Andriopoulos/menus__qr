/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://yourdomain.com", // replace with your real domain
  generateRobotsTxt: true, // will generate robots.txt automatically too
};
